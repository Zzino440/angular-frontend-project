import { PreventNumbersDirective } from './prevent-numbers.directive';

describe('PreventNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
