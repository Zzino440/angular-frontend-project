import { DatasourcePipe } from './datasource.pipe';

describe('DatasourcePipe', () => {
  it('create an instance', () => {
    const pipe = new DatasourcePipe();
    expect(pipe).toBeTruthy();
  });
});
